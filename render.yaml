services:
  - type: web
    name: forge-gym-app
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python wsgi.py
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
